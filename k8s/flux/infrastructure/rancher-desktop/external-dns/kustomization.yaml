apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base/external-dns
  - external-dns-with-openwrt-webhook.yaml

configMapGenerator:
  #ConfigMap used for the webhook sidecar container
- name: external-dns-webhook-env
  namespace: locavora-infra
  envs:
  - default-webhook-env.properties